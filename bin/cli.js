#!/usr/bin/env node

/**
 * CLI entry point for @presto-ai/google-workspace-mcp
 *
 * This simply loads the bundled MCP server which handles
 * authentication and server startup automatically.
 */

require('../dist/index');
